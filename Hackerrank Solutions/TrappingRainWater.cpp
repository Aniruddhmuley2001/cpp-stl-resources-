#include <bits/stdc++.h>
using namespace std;
#define ll long long int

int main()
{
    ll t;
    cin >> t;
    while(t--)
    {
        ll n , c = 0;
        cin >> n;
        vector< ll > v(n);
        for(ll i=0;i<n;i++)
        {
            cin >> v[i];
        }
    }
}